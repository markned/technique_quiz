# Скопируйте в `.env` или `.env.local`. Необязательно: в production без переменных
# подставляются значения по умолчанию из `src/analytics.ts`.
#
# Google Analytics 4 — ID потока (G-XXXXXXXXXX)
# VITE_GA_MEASUREMENT_ID=G-6F2LVVCEY6
# Яндекс.Метрика — номер счётчика
# VITE_YM_COUNTER_ID=108296594
VITE_GA_MEASUREMENT_ID=
VITE_YM_COUNTER_ID=
